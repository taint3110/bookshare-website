export enum EOrderStatusEnum {
  NEW = 'new',
  ORDERED = 'ordered',
  READY = 'ready',
  DONE = 'done',
  FAILED = 'failed',
  CANCELED = 'canceled',
  RETURNED = 'returned',
  REFUNDED = 'refunded',
  OVERDUE = 'overdue'
}
