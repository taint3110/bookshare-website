export enum EOrderStatusEnum {
  NEW = 'new',
  READY = 'ready',
  DONE = 'done',
  FAILED = 'failed',
  CANCELED = 'canceled',
  RETURNED = 'returned',
  REFUNDED = 'refunded',
  OVERDUE = 'overdue'
}
